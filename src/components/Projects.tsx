import React from "react";

const Projects = () => {
  return (
    <section
      className="w-full page-height bg-blue-200 mt-[70px] p-10"
      id="projects"
    >
      <div>Projects</div>
    </section>
  );
};

export default Projects;
